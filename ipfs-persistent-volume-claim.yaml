apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ipfs-persistent-volume-claim
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: fabric-sc
  resources:
    requests:
      storage: 25Gi